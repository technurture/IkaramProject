# Alumni Community Platform - Environment Variables

# Database Configuration
MONGODB_URL=mongodb://localhost:27017/alumni-platform
# For MongoDB Atlas, use:
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/alumni-platform

# Cloudinary Configuration (for file uploads)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Session Security (Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=your-super-secret-session-key-here-replace-with-64-byte-hex-string

# Environment
NODE_ENV=development

# Optional: Custom Port (default: 5000)
PORT=5000

# Base URL for the application (auto-detected if not set)
WEB_URL=http://localhost:5000
# For production, use your actual domain:
# WEB_URL=https://yourdomain.com
# WEB_URL=https://your-project.replit.app